# Everything below is OPTIONAL. The app works fully with none of it set.

# Set this to have the model write the prose instead of the local style rules.
# Get one at console.anthropic.com. It stays on the server, never in the browser.
ANTHROPIC_API_KEY=

ASSISTANT_MODEL=claude-sonnet-4-6
PORT=4000
